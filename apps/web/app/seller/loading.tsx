import React from "react";
import { PageLoader } from "@/components/ui/loader";

export default function SellerLoading() {
  return <PageLoader title="Merchant Ledger" subtitle="Calibrating inventory database parameters..." />;
}
